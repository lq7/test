#SPDX-License-Identifier: MIT-0
---
# tasks file for roles/playtest
- name: PING
  ansible.builtin.ping: {}
- name: 渲染模板
  ansible.builtin.template:
    src: vhost.conf
    dest: "/tmp/vhost-{{ domain.split('.')[0] }}.conf"